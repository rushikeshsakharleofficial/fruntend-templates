# Button System

Variants: primary, secondary, outline, ghost, destructive, link, icon-only.

States: default, hover, active, focus-visible, disabled, loading.

Prompt: Generate a complete button system with semantic HTML, accessible focus states, loading states, icons, and dark/light mode tokens.
